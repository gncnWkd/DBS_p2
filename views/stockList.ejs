<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>종목 목록</title>
</head>
<body>
  <h1>현재 종목 목록</h1>
  <table border="1">
    <tr>
      <th>종목명</th>
      <th>현재 가격</th>
      <th>전일 종가 대비 등락률</th>
    </tr>
    <% stockList.forEach(stock => { %>
    <tr>
      <td><%= stock.StockName %></td>
      <td><%= stock.CurrentPrice %></td>
      <td><%= ((stock.CurrentPrice-stock.PreviousClosingPrice) / stock.PreviousClosingPrice * 100).toFixed(2) %>%</td>
    </tr>
    <% }) %>
  </table>
  <a href = "/home">홈으로 돌아가기</a>

</body>
</html>